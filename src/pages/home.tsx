import React from 'react';
import type {PropsWithChildren} from 'react';

import {SafeAreaView, View} from 'react-native';

type Props = PropsWithChildren<{}>;

export function HomeScreen({}: Props): React.JSX.Element {
  return <View></View>;
}
